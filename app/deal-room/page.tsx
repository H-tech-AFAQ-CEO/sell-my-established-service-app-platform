'use client'
import { useState } from 'react'
import { Check, LockKeyhole, Send } from 'lucide-react'
import { asset } from '@/lib/a2z-data'
import { CheckItem, PageHeader, Shell } from '@/components/a2z-site'
export default function DealRoomPage(){const [sent,setSent]=useState(false); return <Shell><PageHeader eyebrow="Private deal room" title="A clean handover for the next operator.">The acquisition is structured to be straightforward: review the asset, submit an indication of interest, then move through escrow and a guided transfer.</PageHeader><div className="mx-auto grid max-w-7xl gap-10 px-5 py-14 lg:grid-cols-[.8fr_1fr] lg:px-8 lg:py-20"><div><div className="grid grid-cols-2 gap-4">{[['Reserve price',asset.reserve],['Target range',asset.target],['Commission','10% success fee'],['Support','2 months included']].map(([a,b])=><div className="rounded-xl border border-border p-5" key={a}><p className="text-xs text-muted-foreground">{a}</p><p className="mt-2 font-semibold">{b}</p></div>)}</div><h2 className="mt-12 text-xl font-semibold">Included in the transfer</h2><ul className="mt-5 flex flex-col gap-4"><CheckItem>Full Next.js source code and deployment handover</CheckItem><CheckItem>a2zedit.com domain, prepaid for two years</CheckItem><CheckItem>Analytics history, SEO assets and content library</CheckItem><CheckItem>Android and Huawei app assets</CheckItem><CheckItem>Two months of technical transition support</CheckItem></ul><div className="mt-10 flex items-center gap-3 text-sm text-muted-foreground"><LockKeyhole className="size-4 text-primary"/> Escrow-led process. No payment is collected here.</div></div><div className="rounded-2xl border border-border bg-secondary/30 p-6 lg:p-8">{sent?<div className="flex min-h-80 flex-col items-center justify-center text-center"><div className="grid size-12 place-items-center rounded-full bg-primary text-primary-foreground"><Check className="size-6"/></div><h2 className="mt-5 text-xl font-semibold">Interest received</h2><p className="mt-2 max-w-sm text-sm leading-6 text-muted-foreground">This mock form is complete. In production, your details would be routed to Afaq Ahmad for follow-up.</p></div>:<form onSubmit={e=>{e.preventDefault();setSent(true)}} className="flex flex-col gap-5"><div><p className="text-xs font-semibold uppercase tracking-widest text-primary">Start a conversation</p><h2 className="mt-3 text-2xl font-semibold">Request the information pack</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Share a little about yourself and your acquisition interest.</p></div><label className="flex flex-col gap-2 text-sm font-medium">Name<input required className="rounded-lg border border-input bg-background px-3 py-2.5 font-normal outline-none focus:ring-2 focus:ring-ring" placeholder="Your name"/></label><label className="flex flex-col gap-2 text-sm font-medium">Email<input required type="email" className="rounded-lg border border-input bg-background px-3 py-2.5 font-normal outline-none focus:ring-2 focus:ring-ring" placeholder="you@company.com"/></label><label className="flex flex-col gap-2 text-sm font-medium">Message<textarea required rows={4} className="resize-none rounded-lg border border-input bg-background px-3 py-2.5 font-normal outline-none focus:ring-2 focus:ring-ring" placeholder="Tell us about your acquisition plans..."/></label><button className="inline-flex items-center justify-center rounded-lg bg-primary px-4 py-3 text-sm font-medium text-primary-foreground"><Send className="mr-2 size-4"/>Submit inquiry</button><p className="text-center text-xs text-muted-foreground">Demo interaction · no data is sent or stored</p></form>}</div></div></Shell>}
