import Link from 'next/link'
import { CheckCircle2, ArrowRight } from 'lucide-react'
import { asset } from '@/lib/a2z-data'
import { CheckItem, PageHeader, SectionTitle, Shell } from '@/components/a2z-site'
export default function OverviewPage(){return <Shell><PageHeader eyebrow="Business overview" title="A focused utility product with an unusually broad surface area.">A2Z Edit combines practical browser tools with a growing SEO footprint. This page is a buyer-friendly summary of what exists today and where the next operator can take it.</PageHeader><div className="mx-auto grid max-w-7xl gap-12 px-5 py-14 lg:grid-cols-[1fr_.8fr] lg:px-8 lg:py-20"><div><SectionTitle title="What is being acquired?"/><p className="leading-7 text-muted-foreground">A2Z Edit is a functional online toolkit for people who need to edit, convert, scan, and organize files without installing desktop software. It is designed around fast, browser-based workflows and discoverable content pages.</p><ul className="mt-8 flex flex-col gap-4"><CheckItem>Next.js 14 application with responsive web UI</CheckItem><CheckItem>90+ tools across image, PDF, OCR and file utilities</CheckItem><CheckItem>English and Arabic OCR capability</CheckItem><CheckItem>Android and Huawei app presence</CheckItem><CheckItem>Domain prepaid through the next two years</CheckItem></ul></div><aside className="rounded-2xl border border-border bg-secondary/40 p-6"><p className="text-xs font-semibold uppercase tracking-widest text-muted-foreground">Snapshot</p><div className="mt-6 flex flex-col gap-5"><div><p className="text-sm text-muted-foreground">Asset</p><p className="font-medium">{asset.name} · {asset.domain}</p></div><div><p className="text-sm text-muted-foreground">Current status</p><p className="font-medium">Pre-revenue, monetization-ready</p></div><div><p className="text-sm text-muted-foreground">Operating cost</p><p className="font-medium">$0 on current Vercel tier</p></div><div><p className="text-sm text-muted-foreground">Seller support</p><p className="font-medium">2 months included</p></div></div><Link href="/deal-room" className="mt-8 inline-flex items-center text-sm font-medium text-primary">See included assets <ArrowRight className="ml-2 size-4"/></Link></aside></div><section className="border-t border-border bg-secondary/20"><div className="mx-auto max-w-7xl px-5 py-14 lg:px-8"><SectionTitle title="Monetization paths"/><div className="grid gap-4 md:grid-cols-3">{[['Display advertising','Activate AdSense across the existing high-intent content surface.'],['Premium workflows','Gate batch processing, larger files and advanced exports behind a simple plan.'],['Affiliate partnerships','Add relevant storage, productivity and document services to utility flows.']].map(([title, text])=><div className="rounded-xl bg-background p-6" key={title}><CheckCircle2 className="size-5 text-primary"/><h3 className="mt-5 font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{text}</p></div>)}</div></div></section></Shell>}
