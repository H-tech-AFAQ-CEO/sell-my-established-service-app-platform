import { FileImage, FileText, ScanText, Wrench } from 'lucide-react'
import { toolGroups } from '@/lib/a2z-data'
import { PageHeader, Shell } from '@/components/a2z-site'
export default function FeaturesPage(){const icons=[FileImage,FileText,ScanText,Wrench];return <Shell><PageHeader eyebrow="Product surface" title="One toolkit. Four high-intent product families.">A2Z Edit is organized around the file tasks people search for every day, making the current feature set easy to understand and expand.</PageHeader><div className="mx-auto max-w-7xl px-5 py-14 lg:px-8 lg:py-20"><div className="grid gap-5 md:grid-cols-2">{toolGroups.map((group,i)=>{const Icon=icons[i];return <article key={group.title} className="rounded-2xl border border-border p-6 lg:p-8"><div className="flex items-start justify-between"><div className="grid size-11 place-items-center rounded-xl bg-primary/10 text-primary"><Icon className="size-5"/></div><span className="rounded-full bg-secondary px-3 py-1 text-xs font-medium text-muted-foreground">{group.count}</span></div><h2 className="mt-7 text-xl font-semibold">{group.title}</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{group.items.map(item=><div key={item} className="rounded-lg border border-border/70 px-3 py-2.5 text-sm text-muted-foreground">{item}</div>)}</div></article>})}</div><div className="mt-14 flex flex-col gap-4 rounded-xl border border-border bg-secondary/30 p-6 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-medium">Platform coverage</p><p className="mt-1 text-sm text-muted-foreground">Responsive web app plus published Android and Huawei apps.</p></div><span className="text-sm font-medium text-primary">Multi-platform ready</span></div></div></Shell>}
